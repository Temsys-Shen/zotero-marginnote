{
  "name": "zotero-mnaddon",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-release.js",
    "dev": "cd src && zip -r -q ../zotero-connector-dev.mnaddon ."
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "html-minifier-terser": "^7.2.0",
    "clean-css-cli": "^5.6.3"
  }
}
